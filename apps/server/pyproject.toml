[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fluxora-server"
version = "0.1.0"
description = "Fluxora server backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.136.1",
    "uvicorn[standard]==0.47.0",
    "aiosqlite==0.22.1",
    "zeroconf==0.149.7",
    "aiortc==1.14.0",
    "httpx==0.28.1",
    "python-multipart==0.0.29",
    "pydantic-settings==2.14.1",
    "slowapi==0.1.9",
    "python-json-logger==4.1.0",
    "keyring==25.7.0",
    "cryptography==47.0.0",
    "argon2-cffi==25.1.0",
    "psutil==7.2.2",
    "sentry-sdk[fastapi]==2.60.0",
    "pymupdf==1.27.2.3"
]

[project.optional-dependencies]
dev = [
    "pytest==8.2.0",
    "pytest-asyncio==0.23.7",
    "httpx==0.28.1",
    "black==24.4.0",
    "ruff==0.15.13"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["routers*", "models*", "services*", "utils*", "database*"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.black]
line-length = 88
target-version = ["py311"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-v"
