# Fluxora — CLAUDE.md

> Rules for AI coding agents working on the Fluxora codebase. Read this fully on every session start. Detail content lives in linked docs — only the rules live here.

---

## Mandatory Agent Rules

> **Before writing a single line of code:**
> 1. Read `AGENT_LOG.md` to understand what's been done and what comes next.
> 2. Read the relevant `docs/` files for the area you're working in (see "Key files" below).
> 3. If you need current shipped state (test counts, migration ranges, milestone status), read `docs/00_overview/current_status.md`.
> 4. **Check CI status on main.** Run `gh run list --limit 5 --branch=main` (or invoke the `/ci-status` skill). If the most recent run on `main` is a `failure`, surface it to the operator before doing anything else — main may be broken from the last push, and starting new work on a red main wastes effort. See `docs/12_guidelines/05_gh_cli_usage.md` for the triage pattern.
>
> **While writing code — keep code and docs in sync:**
> Follow `docs/12_guidelines/02_documentation_update_protocol.md`. Never stop after updating just the obvious file.
>
> **After the operator pushes commits:**
> Re-run `gh run list --limit 5 --branch=main` ~30 seconds after the push to confirm CI is green. If any run is `failure`, pull the failing log with `gh run view <id> --log-failed | grep -iE "(error|fail|fatal|assertionerror|exception)" | head -40` and surface the cause to the operator. Don't end the session with a red CI run silently sitting on `main`.
>
> **Before ending your session:**
> 1. Append a new entry to `AGENT_LOG.md` following the canonical entry format spec in `docs/12_guidelines/04_agent_log_format.md`.
> 2. List every file you created or modified (code **and** docs) in the entry's `### Files Created / Modified` table (3-column: Action / Path / Why).
> 3. List every `docs/` file you updated in the `### Docs Updated` section of the log entry.
> 4. Write a clear `### Next Agent Should` section so the next agent can resume without reading chat history.
> 5. **Report Issues:** if you discover security errors, sharp edges, or improvement opportunities during your work, you MUST explicitly report them to the user.
> 6. **Proactive Suggestions:** offer 2–3 prioritised suggestions for what to work on next based on `AGENT_LOG.md` + the roadmap + your findings.
>
> **AGENT_LOG.md is append-only** — never edit or delete past entries.
> **Log Rotation Policy:** if `AGENT_LOG.md` exceeds ~1000 lines, archive it to `docs/logs/AGENT_LOG_archive_XX.md`, summarize, and start a fresh log with the summary at the top.
>
> **When spawning audit subagents:** if your subagent flags code for deletion (e.g. an "orphaned" screen, an unused export), it MUST first grep the surrounding files for an explicit "kept-on-purpose" / "stays in tree" / "v1.1 stub" comment. False-positive deletions waste a round-trip; the comments tell you whether the code is dead or deliberately gated.

---

## Hard Prohibitions

| # | Rule |
|---|------|
| 1 | **Never run `git commit`, `git push`, or any git write command.** All version control is the owner's responsibility. You may read git state (`git status`, `git log`, `git diff`) but must never write to it. |
| 2 | **Never add agent branding anywhere.** No comments, docstrings, README badges, footer text, or any other content that names, credits, or promotes an AI model (e.g. "Generated by Claude", "Built with Gemini", "AI-assisted"). Code must read as if a human wrote it. |
| 3 | **Never use `print()` in Python or `print()` / `debugPrint()` in Dart.** Use the project logger in every file. Silent output is invisible in production. |
| 4 | **Never swallow exceptions silently.** No bare `except: pass` in Python, no empty `catch (_) {}` in Dart. Always log the error with full context and either rethrow or handle explicitly. |
| 5 | **Never hardcode secrets, credentials, ports, or file paths.** All config lives in `config.py` via `BaseSettings` (Python) or is injected via the DI layer (Dart). No magic strings or numbers outside config / constants files. |
| 6 | **Never add a new pub / pip dependency without justification.** Check if an existing dependency already covers the need. Every new dep is a maintenance burden and supply-chain risk. |
| 7 | **Never break Clean Architecture layer boundaries.** A layer violation that compiles is still a bug. See the Architecture Rules section in `docs/12_guidelines/01_development_guidelines.md`. |
| 8 | **Never log tokens, passwords, or any PII.** Scrub sensitive fields before logging. If a log line could expose a bearer token or user path, it must not be written. |
| 9 | **Never edit or delete a past database migration file.** Migrations are append-only. |
| 10 | **Never use string concatenation to build SQL queries.** Always use parameterized queries. |
| 11 | **Never commit API keys, Firebase config files, or any secret.** `google-services.json`, `GoogleService-Info.plist`, `.env`, `config.json` (dart-define) are gitignored and provided at build time only. |
| 12 | **Never use a package version without first checking if a newer version exists.** Before adding or updating any pip / pub / npm / GitHub Action dependency, look up the current latest release. Do not pin to a version you found in training data — it is likely outdated. |
| 13 | **Never store bearer tokens as plaintext or reversibly encrypted in the database.** Bearer tokens must be stored as HMAC-SHA256 hashes. Other secrets (API keys) use Fernet encryption. |

---

## What is Fluxora?

A **self-hosted hybrid media streaming system** — "Plex meets Syncthing." Streams a personal media library (movies, TV, music, documents) to any device, automatically switching between LAN (fast, direct) and Internet (WebRTC / STUN / TURN) without user intervention.

Components:
1. **Server** — Python / FastAPI backend on the user's home machine; transcodes via FFmpeg; broadcasts via mDNS.
2. **Mobile Client** — Flutter app (iOS + Android) that discovers, pairs, streams.
3. **Desktop Control Panel** — Flutter desktop (Windows / macOS / Linux) for libraries, clients, server settings.
4. **Shared Core** — `packages/fluxora_core` — shared entities, network client, design tokens, secure storage.
5. **Web Landing** — Next.js static site at `fluxora.marshalx.dev`.

**Key constraint:** core streaming works with zero cloud dependency. LAN streaming never touches Firebase. Firebase is used only for Phase 3+ features (WebRTC signaling, push, subscriptions) and degrades gracefully when absent. Server is distributed as a standalone PyInstaller executable. No Docker, no external DB, no mandatory account for Phase 1–2.

---

## Where the detail lives

| If you need... | Read |
|----------------|------|
| Current shipped state (tests, migrations, features) | `docs/00_overview/current_status.md` |
| Repo layout / folder tree | `docs/00_overview/folder_structure.md` |
| Tech stack, code conventions, architecture rules, security rules, dev commands, dep policy, secrets handling | `docs/12_guidelines/01_development_guidelines.md` |
| Documentation update protocol | `docs/12_guidelines/02_documentation_update_protocol.md` |
| Known risks & gotchas | `docs/12_guidelines/03_gotchas.md` |
| AGENT_LOG.md entry format (canonical structure for log entries) | `docs/12_guidelines/04_agent_log_format.md` |
| Roadmap / phase status | `docs/10_planning/01_roadmap.md` |
| Locked architectural decisions (ADRs) | `docs/10_planning/02_decisions.md` |
| Manual / external tasks tracker | `docs/10_planning/04_manual_tasks.md` |
| Ship readiness — what blocks v1 launch | `docs/10_planning/05_ship_readiness.md` |
| Installer & distribution plan (proposed, awaiting decision) | `docs/10_planning/06_installer_plan.md` |
| Streaming pipeline issue audit + remediation plan (seek-restart, tonemap timeout, zombie cleanup) | `docs/10_planning/11_streaming_pipeline_issues.md` |
| Streaming resume + throttle remediation plan (server-side resume seek + `-readrate 1.5` + mobile buffer tuning + audio diagnostics; sliding-window deferred to v1.1) | `docs/10_planning/16_streaming_resume_and_throttle_plan.md` |
| FFmpeg diagnostics + M2 retry plan (uniform `info` loglevel + `services/ffmpeg_capabilities.py` version probe + capability-gated transcode-only `-readrate` + same-day real-device follow-on patches) | `docs/10_planning/17_ffmpeg_diagnostics_and_m2_retry_plan.md` |
| User-driven library transcode plan — opt-in AV1/VP9 → H.264 sidecars so playback stream-copies; post-scan toast + multi-select UI + background queue worker | `docs/10_planning/18_library_transcode_plan.md` |
| Library transcode follow-ups — plan 19 (all 8 milestones shipped 2026-05-09: client-side decoding default + quality preset chooser + storage mode/cache root + storage strip + folder tree + per-row affordances + edge-case hardening + per-library overrides), archived 2026-05-09 | `docs/10_planning/archive/19_library_transcode_followups.md` |
| Auto streaming mode — plan 20 (`streaming_mode='auto'` opt-in third value with transparent stream-copy → transcode fallback within 6 s of `PlayerReady`; per-client codec blocklist; `POST /fallback-transcode` endpoint; `stream_decision` diagnostic log), archived 2026-05-12 | `docs/10_planning/archive/20_auto_streaming_mode.md` |
| Client-side audio decoding — plan 21 (audio stream-copy for `{aac, ac3, eac3, opus, flac}` allowlist; fmp4 switch for non-AAC audio; separate `client_audio_codec_blocklist` table; new `POST /fallback-audio-transcode` endpoint for audio-only fallback; 128k → 256k AAC bitrate bump + `-ac` channel preservation on the re-encode paths that remain; M1-M5 ~12 h), shipped 2026-05-12 | `docs/10_planning/archive/21_client_audio_decoding.md` |
| Multi-audio-track support — plan 22 (`-map 0:v:0 -map 0:a?` under stream-copy; init.mp4 declares every track; `_probe_audio_tracks` helper + `_session_audio_tracks` cache; mixed-codec fallback (`audio-mixed-codec-fallback` reason); migration 035 adds `media_files.audio_tracks` JSON; `AudioTrackInfo[]` on `/stream/start`; mobile Audio sheet picker with media_kit client-side track switching; M1-M5 ~10 h), shipped 2026-05-14 | `docs/10_planning/archive/22_multi_audio_track_support.md` |
| Server-restart audio track switching — plan 23 (`POST /api/v1/stream/{session_id}/audio-track` respawns FFmpeg with `-map 0:a:<index>?` + unlinks stale init.mp4 + returns segment-snapped `applied_seek_sec`; cubit re-opens playlist; libmpv `ao=audiotrack` override + screen-lifetime `WakelockPlus` for Android audio reliability; **effectively superseded by plan 24** — endpoint stays as future-client fallback), shipped 2026-05-15 | `docs/10_planning/archive/23_audio_track_switching.md` |
| Android ExoPlayer migration — plan 24 (**Active 2026-05-15; M1-M4 + M7 + M8 + M9-partial + M9-partial follow-ons shipped same day** — Kotlin spike + `PlayerEngine` Dart abstraction in `packages/fluxora_core/lib/player/` + `ExoPlayerEngine` Dart talking to MethodChannel `dev.marshalx.fluxora/exo_player` + Kotlin module with `Player.Listener` events, 250ms position ticker, audio focus, becoming-noisy auto-pause, bearer-header injection without logging values, source-index→`(TrackGroup, formatIndex)` mapping + Media3 `MediaSessionService` for lockscreen / notification / BT transport + `ExoPlayerEngine.open()` state-reset emission + scrubber pin gate fix + `setMetadata(title)` plumbed to the lockscreen + subtitle tab gating + dev spike removed + `_kEnableExoPlayerEngine` flag dropped. **Real-device follow-ons (post-`24d3579`):** `network_security_config.xml` for LAN cleartext (global allow + `fluxora-api.marshalx.dev` HTTPS-only carve-out) since Media3's `DefaultHttpDataSource` honours Android 9+ default-secure cleartext policy that libmpv ignored; client-side HDR→SDR tonemap via custom `TonemappingRenderersFactory` (subclasses `DefaultRenderersFactory`; `KEY_COLOR_TRANSFER_REQUEST = COLOR_TRANSFER_SDR_VIDEO` on API 33+; server-side `?tonemap=true` remains fallback); `PlayerEngine.videoSize` + `videoSizeStream` (emitted from `onVideoSizeChanged` + `onTracksChanged`; PAR baked in for anamorphic sources); three-way `FitMode {fit, fill, stretch}` cycling top-bar button (default `fit`); pinch zoom via raw `Listener` (Flutter's gesture arena loses 2-finger scale at slop crossover; raw pointer-distance math + `_activePointers >= 2` gating); drag-HUD `zoom` + `fitMode` cases with `"1.25×"` / mode-name labels; player UI relayout (transport below scrubber, `_MinimizeHandle` removed, gradient chrome backdrops); overflow-menu scroll fix (`SingleChildScrollView` + `isScrollControlled: true`). Media3 1.10.1 (`-exoplayer`, `-exoplayer-hls`, `-ui`, `-session`). Android constructs `ExoPlayerEngine` unconditionally; libmpv stays desktop + iOS engine + Android operator escape hatch via `_kForceMediaKitOnAndroid` (kept until M5+M6 device-green). M5 (multi-audio device smoke) + M6 (HDR + tonemap) require operator device testing. M9 finishes with `_kForceMediaKitOnAndroid` deletion + plan archival once M5+M6 confirm. iOS deferred; multi-rendition HLS server output split into plan 25) | `docs/10_planning/24_player_audio_reliability_plan.md` |
| Client Groups UI gap audit + 5-milestone remediation plan (decorative create/edit dialogs, raw-UUID add member, mobile 403 UX) | `docs/10_planning/12_groups_remediation_plan.md` |
| Client Groups v2 redesign — content-spaces model + Public group + PIN-gated groups + per-group caps + view-as debug | `docs/10_planning/13_groups_v2_content_spaces.md` |
| Client Groups dedicated management page — replaces Create/Edit modal with a 6-tab page (Overview / Members / Access / PIN / Activity / View As) | `docs/10_planning/14_groups_management_page.md` |
| Mobile settings (Profile-as-settings) — completed 6-milestone remediation plan, archived 2026-05-08 | `docs/10_planning/archive/15_mobile_settings_remediation_plan.md` |
| Library file-browser power features — plan 28 (🔵 In Progress 2026-05-16; **Phase A + B ✅ shipped 2026-05-16** — commits `65a3555` server + `f0971ae` client A + `6fdfde4` client B).  **Phase A landed:** server `_attach_index_status` rewritten as one LEFT-JOIN against `media_files` + `media_thumbnails` + `EXISTS(stream_sessions...)` exposing per-indexed-entry `media` payload (width/height/duration_sec/codec_name/hdr_format/audio_codec/thumbnail_status/thumbnail_generated_at_unix/indexed_at_iso/is_streaming) + `mtime_unix`.  Stale-thumbnail auto-re-queue (source.updated_at > thumbnail.generated_at flips worker row back to pending priority=5 + reports `thumbnail_status='stale'`).  Client gains 320 px `LibraryBrowseDetailPanel` (kind header + thumbnail preview + path card + 2-col stats + media block + Open/Reveal/CopyPath/StreamTest actions); `LibraryBrowseSearchBar` (compact TextField filtering current dir by name substring; Phase B added optional `focusNode` param for screen-level `/` shortcut wiring); `LibraryBrowseViewToggle` (segmented control).  Single-click-selects + double-click-opens via `_lastTapAt` 300 ms pattern.  Sortable column headers (NAME / SIZE / MODIFIED).  List ↔ Grid view (grid tiles 180×200 with real thumbnails via `/api/v1/files/{id}/thumbnail?v=<unix>`).  HDR row pill + ▶-live row pill.  Distinct empty-state copy per scenario.  Cubit gains `BrowseSortColumn` + `BrowseViewMode` enums + selection / sort / view / search / indexed-only state + top-level `applyBrowseFilters` pure function.  Server suite 916 → 925 (+9).  **Phase B landed:** type-filter chip row (`LibraryBrowseFilterChips`: All / Folders / Videos / Images / Audio / PDFs / Other single-select kind chips + independent "Indexed only" toggle that replaces the Phase A toolbar checkbox); item-count footer (`LibraryBrowseCountFooter`, `kHeight=28` const) at the panel bottom showing post-filter `N items · M folders, K files`; keyboard navigation on the listing — `_LibraryBrowseView` converted to `StatefulWidget` with `Focus(autofocus: true, onKeyEvent: _onKey)` on the body routing ↓↑ → `stepSelection(±1)`, PageDown/Up → `stepSelection(±10)`, Home/End → `selectFirst/Last`, Enter → `_openSelected` (`resolveSelected()` then directory navigate vs file open), Backspace → `goUp`, Escape → `clearSelection`, `/` → `_searchFocus.requestFocus()`.  Handler swallows only the keys it consumes via `KeyEventResult.handled`; other keystrokes propagate so text fields keep working.  Cubit gains `BrowseKindFilter enum { all, folders, videos, images, audio, pdfs, other }` + `setKindFilter` + `selectOnly` / `clearSelection` / `stepSelection(delta)` / `selectFirst` / `selectLast` / `resolveSelected` helpers; `applyBrowseFilters` extended with `BrowseKindFilter kindFilter` named param defaulting to `.all` (back-compat).  Polish: failed-thumb warning icon overlay (`Icons.warning_amber_rounded` in `AppColors.amber`) on `_BrowseRow` + `_BrowseGridTile` when `media.thumbnail_status == 'failed'`; new `_IndexedTag` widget with formatted `indexed_at` timestamp tooltip (replaces Phase A's bare "Indexed" pill).  Long-hover quick-preview popover **deliberately skipped** — right detail panel already shows the same info on click; parallel overlay was marginal value for Overlay z-index cost.  Desktop suite **121** (widget tests for chips + footer + key handler land with Phase C).  **Phase C (right-click context menu + editable path textbox + per-file Index/Re-thumb endpoints + density toggle + multi-select Ctrl/Shift) / D (back/forward history + lazy folder-size) still pending.**  Tier 3 features (recursive search via FTS5, bookmarks, drag-and-drop to Convert, filesystem watching via `watchdog`) split out as separate plans. | `docs/10_planning/28_library_file_browser_power_features.md` |
| Per-file thumbnail generation — plan 27 (✅ all six milestones shipped 2026-05-16, commits `217ff8b` M1 → `137362c` M2 → `3dbaf16` M3 → `b0428b0` M4 → `3e8066e` M5; doc sweep + AGENT_LOG in M6.  **Post-ship 2026-05-16:** speed bump (CONCURRENCY 2→4, `-hwaccel auto` w/ software-fallback retry, lanczos→bilinear, `q:v 5→8`); real-time progress visibility via new `thumbnails_progress` WS event + per-library progress bar on library cards; `LibraryRepositoryImpl._resolveCoverUrls` fix for server-relative cover_urls (Flutter `Image.network` can't fetch relative paths — TMDB URLs worked because absolute, generated-thumb URLs are `/api/v1/...`); folder-browser MVP rewrite of `library_files_screen.dart` w/ new `GET /api/v1/library/{id}/browse` endpoint + `BrowseService` (path-traversal-secured, hidden-file detection, `media_files.path` JOIN for `is_indexed`/`file_id`).  Background asyncio worker (default CONCURRENCY=2) consumes the new `media_thumbnails` queue populated by the scan path via `INSERT OR IGNORE`; atomic claim via `UPDATE ... RETURNING` ordered by `priority DESC, created_at ASC`; operator-opened library (`GET /files?library_id=X`) bumps that library's pending thumbs to priority=10.  Four extractor paths in `services/thumbnail_service.py`: FFmpeg for video w/ Hable HDR→SDR tonemap branch when `media_files.hdr_format IS NOT NULL`, FFmpeg for image (JPEG/PNG/WEBP/HEIC), FFmpeg `-vcodec copy` for audio embedded APIC (skipped when no embedded art — most CD rips), PyMuPDF for PDF first page (skipped if encrypted, failed if corrupt; lazy import so missing wheel surfaces as per-file skip not startup crash).  Failure aggregation: one notification per library at ≥ 5 permanent failures, dedup against open `(category='thumbnail', dismissed_at IS NULL)`.  New endpoints `GET /api/v1/files/{file_id}/thumbnail?v=<unix>` (cached JPEG w/ 1-day public Cache-Control; 404 when not ready or missing-on-disk; visibility-gated 404 for cross-group bearer callers) + `POST /api/v1/library/{id}/regenerate-thumbnails` (resets rows + deletes JPEGs; records `library.thumbnails_regenerated` activity event).  `_library_aggregates` unions TMDB poster_url URLs (preferred) with thumbnail endpoint URLs up to 4 cover slots; excludes files that already have TMDB art to avoid duplicates; `?v=<gen_unix>` cache-buster invalidates Flutter `cached_network_image` / browser caches automatically on regeneration.  Operator-configurable `user_settings.thumbnail_width` (160–640 px, default 320; Pydantic `Field(ge=160, le=640)` returns 422 on out-of-range).  Desktop "Regenerate Thumbnails" `_ActionTile` on library detail panel + Settings → Advanced → Thumbnails card with `FluxSlider`.  New pip dep: `pymupdf==1.27.2.3` (pure-Python wheel, AGPL-compatible with self-hosted distribution).  Three migrations (037 `media_thumbnails` table + 038 widens `notifications.category` CHECK to include `'thumbnail'` + 039 `user_settings.thumbnail_width`).  On-demand endpoint generation explicitly deferred — BG worker is the only generation path; endpoint 404 → client gradient mosaic fallback.  Orphan-JPEG sweeper runs every 6 h (bounded to 100 deletions/pass).  Crash-recovery resets prior-session `generating` rows back to `pending` at startup.  Test counts: server **814 → 898 (+84)** across M1–M4 + pre-existing-uncounted paths; desktop **118 → 121 (+3)** for the regenerate cubit. | `docs/10_planning/27_thumbnail_generation_plan.md` |
| Desktop Control Panel IA redesign — plan 26 (10 → 7 flat rail; M1–M5 shipped 2026-05-15 + **2026-05-16 refinement landed:** Library tabs are Libraries / Convert / Transcoding (Transcoding moved in from Activity — live encoder dashboard belongs with Convert workflow); Activity tabs are Sessions / Logs only; Scan-history tab dropped (no real content); `LibraryShell` + `ActivityShell` rewritten as `StatefulWidget` + `IndexedStack` tab hosts with `_visited: Set<TabPath>` lazy-mount so unvisited tabs' cubits never construct (was per-tab `go_router` routes that tore down the shell on every tab click — operator caught the pop-in/out on real-device); `LibraryCubit` + `StorageCubit` promoted to GetIt lazy singletons + `BlocProvider.value` injection so navigating away from `/library` and back reuses cached state; new public `refresh()` methods emit straight to `*Loaded` for stale-while-revalidate; `didChangeDependencies` runs auto-select + `cubit.refresh()` against pre-existing cached state; new `LibraryEventsService` WebSocket subscriber over `/api/v1/ws/notifications` demuxes ephemeral `{"type":"event","kind":"library_changed"\|"storage_changed"}` frames into broadcast streams the cubits consume (replaces 15 s polling timers; server-side `notification_service.broadcast_event(kind)` helper + `apps/server/routers/library.py` fires events after every mutation); new `FluxPillTabs` shared widget + `PageHeader.verticalPadding` param; `LibraryScreen` polish (alphabetical pre-selection, sandwich gradient on cards, faded type-icon fallback, manual double-tap via `_lastTapAt` since Flutter `onDoubleTap` introduced 300 ms single-tap latency, skeleton-loading body, Add Library auto-populates name from folder basename); `TranscodeScreen` right-panel rework + Candidates sortable flat table with pagination + Status column removed + "Output size" rename; back-compat redirects `/transcode` → `/library/convert`, `/transcoding` → `/library/transcoding` (target updated 2026-05-16), `/logs` → `/activity/logs` kept until v1.1 cleanup; desktop test count 114 → 118) | `docs/10_planning/26_desktop_cp_ia_redesign.md` |
| URL inventory (every endpoint, WS, hosted, third-party, future TBD) | `docs/05_infrastructure/02_url_inventory.md` |
| Design system | `DESIGN.md` |

### Key files to read before specific tasks

| Task | Read first |
|------|------------|
| Any UI work | `DESIGN.md` |
| Adding an API endpoint | `docs/04_api/01_api_contracts.md` |
| Changing DB schema | `docs/03_data/02_database_schema.md` |
| Backend service changes | `docs/09_backend/01_backend_architecture.md` |
| Flutter screen / widget | `docs/08_frontend/01_frontend_architecture.md` |
| Networking / streaming | `docs/02_architecture/01_system_overview.md` (architecture); `docs/10_planning/11_streaming_pipeline_issues.md` (current defects + remediation plan) |
| Security-sensitive feature | `docs/06_security/01_security.md` |
| CI / CD changes | `docs/05_infrastructure/01_infrastructure.md` |
| MCP servers (Dart / fluxora-db) — setup, DTD URI workflow, slowness gotchas | `docs/12_guidelines/06_mcp_setup_and_usage.md` |
| Claude Code skills (archive-plan / ci-status / doc-sweep / new-plan) — when to invoke + how to add new ones | `docs/12_guidelines/07_skills_reference.md` |

---

## Out of scope (v1)

Light mode · multi-user accounts · cloud backup / remote library · music streaming UI · torrent integration · browser-based web client · subtitle / caption rendering (Phase 3+) · AI-based recommendations (Phase 5).
